import { View, ViewParams } from "skydapp-common";
export default class SupernovaEvent implements View {
    private container;
    private eventList;
    constructor();
    private init;
    private loadEvent;
    changeParams(params: ViewParams, uri: string): void;
    close(): void;
}
//# sourceMappingURL=SupernovaEvent.d.ts.map