import { View, ViewParams } from "skydapp-common";
export default class GaiaDividend implements View {
    private container;
    private list;
    constructor();
    private load;
    changeParams(params: ViewParams, uri: string): void;
    close(): void;
}
//# sourceMappingURL=GaiaDividend.d.ts.map